import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: { flex: 1 },
  eachScreenContainerStyle: {
    marginHorizontal: 17,
    marginVertical: 24,
  },
});
