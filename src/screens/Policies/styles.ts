import { StyleSheet } from 'react-native';
import * as Color from '@config/colors';

export const mainPolicyScteen = StyleSheet.create({
  container: {
    flex: 1,
    flexDirection: 'column',
    marginTop: 8,
  },
});

export const commonPolicyScreenStyle = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Color.White,
    paddingHorizontal: 20,
  },
});
