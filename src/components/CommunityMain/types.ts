export type CommunityMainProps = {
  enterContenetDetail: () => void
};

export type ContentItem = {
  // id: number;
  title: string;
  postAuthor: string;
  postingDate: string;
  imageSrc: string;
  commentNum: number;
}