export type LectureImageProps = {
  img: string;
};

export type LectureContentsProps = {
  id: number;
  title: string;
  organization: string;
  level: string;
  region: string;
};

export type CommonLectureProps = {
  id: number;
  title: string;
  organization: string;
  level: string;
  region: string;
  imageUrl: string;
};
